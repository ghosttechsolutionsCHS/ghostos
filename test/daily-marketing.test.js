import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const src=fs.readFileSync(new URL('../src/daily-marketing.js',import.meta.url),'utf8');const fn=fs.readFileSync(new URL('../netlify/functions/daily-marketing-cycle.js',import.meta.url),'utf8');const api=fs.readFileSync(new URL('../netlify/functions/marketing-approval.js',import.meta.url),'utf8');const ui=fs.readFileSync(new URL('../public/marketing-approvals.html',import.meta.url),'utf8');
test('daily engine enforces owner review and truthful publishing state',()=>{assert.match(src,/Owner Decision':'Pending/);assert.match(src,/Approved — Publishing Connection Required/);assert.match(src,/published:false/);assert.match(src,/No external action taken/);assert.doesNotMatch(src,/Published — Connected Confirmation'\s*:/)});
test('ECHO daily contract includes feed, story, creative, CTA and non-fabrication',()=>{for(const x of ['at least one feed post and one Story','creative brief','caption','CTA','hashtags','recommended time','never present it as a real customer/result'])assert.ok(src.includes(x),x)});
test('SCOUT verifies group rules and blocks uncertain posting claims',()=>{for(const x of ['verify available rules from live public evidence','Posting Confidence','Never pose as a customer','never imply posting is allowed'])assert.ok(src.includes(x),x)});
test('BEACON uses Windsor funnel sources and economical Clarity reads',()=>{for(const x of ['GA4, Search Console, Google Business Profile and Microsoft Clarity','Clarity is request-limited','do not repeatedly poll it','If Search Console has no rows'])assert.ok(src.includes(x),x)});
test('Google Ads authorization is exactly $10 daily and $70 first seven days with no fake launch',()=>{assert.match(src,/dailyMax:10,firstSevenDaysMax:70,radiusMiles:25/);assert.match(src,/current GhostOS Google Ads connection is read-only/i);assert.match(src,/Never disguise prohibited consumer repair\/technical-support services or circumvent policy/);assert.match(src,/No Meta budget is authorized/)});
test('marketing money learning uses completed-job economics',()=>{assert.match(src,/spend -> lead -> booked job -> completed job -> revenue -> parts cost -> gross profit -> CAC\/cash/);assert.match(src,/Engagement without customers is not success/)});
test('daily schedule and iPhone approval UI exist',()=>{assert.match(fn,/schedule:'15 11 \* \* \*'/);assert.match(api,/path:'\/api\/marketing-approval'/);for(const x of ['Owner Marketing Approval','APPROVE','REJECT','APPROVE EDITED VERSION','$10/day · $70 first 7 days'])assert.ok(ui.includes(x),x)});
test('preserved safety boundaries remain explicit',()=>{for(const x of ['organicAutoPublish:false','customerAutoSend:false','automaticPurchases:false','metaSpendAuthorized:false'])assert.ok(src.includes(x),x)});
