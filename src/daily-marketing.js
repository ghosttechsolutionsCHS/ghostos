import { z } from 'zod';
import { runAgent, defineTool } from './ai-provider.js';
import { TABLES, createRecord, listRecords, updateRecord, logActivity } from './airtable.js';
import { echo, scout, beacon, forge, horizon } from './growth.js';
import { getMondayMarketingSnapshot } from './monday-marketing.js';
import { validateExecutionSpec } from './marketing-execution.js';
import { creativePublicUrl } from './creative-provider.js';

const TZ='America/New_York';
const DAILY_AD_AUTH=Object.freeze({dailyMax:10,firstSevenDaysMax:70,radiusMiles:25,market:'Charleston / North Charleston, SC',metaBudgetAuthorized:false});
function localDate(d=new Date()){return new Intl.DateTimeFormat('en-CA',{timeZone:TZ,year:'numeric',month:'2-digit',day:'2-digit'}).format(d)}
function nextLocalDate(d=new Date()){return localDate(new Date(d.getTime()+86400000))}
function clean(v,max=30000){return String(v||'').slice(0,max)}
function selectName(v){return typeof v==='string'?v:v?.name||''}
function isMarketingItem(row){const f=row.fields||{};return Boolean(f.Platform||f['Content Type']||f.Fingerprint)}
function forDate(row,date){return String(row.fields?.['Planned Date']||'').slice(0,10)===date}
function pending(row){return selectName(row.fields?.['Owner Decision'])==='Pending'||(!row.fields?.['Owner Decision']&&['Needs Owner','Ready for Owner'].includes(selectName(row.fields?.Status)))}
function executionPayload(raw){if(!raw)return null;try{return typeof raw==='string'?JSON.parse(raw):raw}catch{throw new Error('executionPayloadJson must be valid JSON')}}
function withOwnerEdit(action,payload,edit){if(!edit||!payload)return payload;const p={...payload};if(action==='create_post')p.message=clean(edit,63206);else if(action==='create_photo_post'||action==='create_image_post'||action==='create_carousel_post'||action==='create_video_post')p.caption=clean(edit,action==='create_photo_post'?63206:2200);return p}
function siteOrigin(){return globalThis.Netlify?.env?.get?.('URL')||process.env.URL||process.env.DEPLOY_PRIME_URL||''}
function automaticSocialExecution(a,assetUrl){const platform=String(a.platform||''),type=String(a.contentType||''),caption=clean(a.draftContent||'',platform==='Facebook'?63206:2200);if(platform==='Facebook'&&!/story/i.test(type))return {connector:'facebook_organic',action:'create_photo_post',payload:{image_url:assetUrl,caption}};if(platform==='Instagram'&&/story/i.test(type)){const multi=/Frame\s*2\s*:/i.test(String(a.draftContent||''));return multi?null:{connector:'instagram',action:'create_story',payload:{image_url:assetUrl}}}if(platform==='Instagram'&&!/story/i.test(type))return {connector:'instagram',action:'create_image_post',payload:{image_url:assetUrl,caption}};return null}

const saveMarketingCardTool=defineTool({
  name:'save_marketing_approval_card',
  description:'Persist one structured marketing card. Owner approval is reserved for consequential publish/spend/reputation decisions. This NEVER itself publishes, sends, purchases, changes an ad account, or spends money.',
  parameters:z.object({agent:z.enum(['ECHO','SCOUT','BEACON','FORGE','HORIZON']),title:z.string().min(3).max(250),workType:z.enum(['Content Draft','Free Acquisition','SEO / Website','Marketing Analysis','Executive Input']),platform:z.string().min(2).max(100),contentType:z.string().min(2).max(100),plannedDate:z.string().regex(/^\d{4}-\d{2}-\d{2}$/),fingerprint:z.string().min(6).max(250),purpose:z.string().min(3).max(10000),draftContent:z.string().max(30000).optional(),creativeBrief:z.string().max(30000).optional(),creativeAssetUrl:z.string().url().optional(),cta:z.string().max(1000).optional(),hashtags:z.string().max(5000).optional(),recommendedPostingTime:z.string().max(200).optional(),evidence:z.string().max(30000).optional(),evidenceUrl:z.string().url().optional(),groupRules:z.string().max(30000).optional(),postingConfidence:z.enum(['High — Rules Support Posting','Medium — Some Rules Unclear','Low — Do Not Post Yet']).optional(),moneyInvolved:z.number().nonnegative().optional(),nextAction:z.string().max(10000).optional(),ownerApprovalRequired:z.boolean().default(true),scheduledAt:z.string().datetime().optional(),executionConnector:z.enum(['facebook_organic','instagram','google_ads']).optional(),executionAction:z.string().max(100).optional(),executionPayloadJson:z.string().max(30000).optional()}),
  async execute(a){
    const existing=await listRecords(TABLES.GROWTH_WORK,{maxRecords:300});const prior=existing.find(r=>String(r.fields?.Fingerprint||'')===a.fingerprint);if(prior)return {id:prior.id,deduped:true,externalActionTaken:false};
    const requestedPayload=executionPayload(a.executionPayloadJson);if(a.executionConnector||a.executionAction||requestedPayload){if(!(a.executionConnector&&a.executionAction&&requestedPayload))throw new Error('Execution connector, action and payload must be supplied together');validateExecutionSpec({connector:a.executionConnector,action:a.executionAction,payload:requestedPayload});}
    const now=new Date().toISOString(),social=['Instagram','Facebook','Instagram + Facebook'].includes(a.platform),key=`${a.fingerprint}:v1`;
    // Legacy regression contract: 'Owner Decision':'Pending' whenever ownerApprovalRequired=true; routine internal work does not become owner homework.
    let rec=await createRecord(TABLES.GROWTH_WORK,{'Growth Item':a.title,Agent:a.agent,'Work Type':a.workType,Status:a.ownerApprovalRequired?'Needs Owner':'Ready for Owner','Current Task':`Prepared for ${a.plannedDate}`,'Latest Result':a.purpose,'Next Action':a.nextAction||'No owner action required unless this card represents a consequential decision.','Evidence / Context':a.evidence||'','Draft Content':a.draftContent||'','Channel / Partner':a.platform,'URL / Contact':a.evidenceUrl,Platform:a.platform,'Content Type':a.contentType,'Creative Brief':a.creativeBrief||'','Creative Asset URL':a.creativeAssetUrl,CTA:a.cta||'',Hashtags:a.hashtags||'','Recommended Posting Time':a.recommendedPostingTime||'',Purpose:a.purpose,'Money Involved':a.moneyInvolved,'Publishing Status':social?'Draft — Owner Review':undefined,'Owner Decision':a.ownerApprovalRequired?'Pending':undefined,'Group Rules':a.groupRules||'','Posting Confidence':a.postingConfidence,'Planned Date':a.plannedDate,Fingerprint:a.fingerprint,'Evidence URL':a.evidenceUrl,'Owner Approval Required':a.ownerApprovalRequired,'Execution Status':a.ownerApprovalRequired?'Awaiting Owner':'Prepared','Execution Connector':a.executionConnector,'Execution Action':a.executionAction,'Execution Payload JSON':requestedPayload?JSON.stringify(requestedPayload):undefined,'Execution Idempotency Key':key,'Scheduled At':a.scheduledAt,'Created At':now,'Updated At':now});
    if(a.agent==='ECHO'&&social&&siteOrigin()){
      const assetUrl=creativePublicUrl(siteOrigin(),rec.id,1),auto=automaticSocialExecution(a,assetUrl);const patch={'Creative Asset URL':assetUrl};
      if(auto&&!a.executionAction){validateExecutionSpec(auto);Object.assign(patch,{'Execution Connector':auto.connector,'Execution Action':auto.action,'Execution Payload JSON':JSON.stringify(auto.payload),'Execution Idempotency Key':key})}
      rec=await updateRecord(TABLES.GROWTH_WORK,rec.id,patch);
    }
    await logActivity({agent:a.agent,actionType:'marketing_card_prepared',status:a.ownerApprovalRequired?'Blocked':'Done',detail:`${a.contentType}: ${a.title}. Owner review required=${a.ownerApprovalRequired}. No external action taken.`,consequential:a.ownerApprovalRequired});return {id:rec.id,deduped:false,externalActionTaken:false};
  }
});
function specialist(agent){return {...agent,tools:[...(agent.tools||[]),saveMarketingCardTool]}}

export async function getMarketingApprovalInbox({date=localDate()}={}){const [work,snapshot]=await Promise.all([listRecords(TABLES.GROWTH_WORK,{maxRecords:300}),getMondayMarketingSnapshot()]);const items=work.filter(isMarketingItem).filter(r=>forDate(r,date)||pending(r)).sort((a,b)=>new Date(b.fields?.['Updated At']||b.fields?.['Created At']||0)-new Date(a.fields?.['Updated At']||a.fields?.['Created At']||0));return {date,greeting:'Good morning. Here is what your company prepared today.',items,summary:{pending:items.filter(pending).length,newLeads:snapshot.metrics.newLeads,bookedJobs:snapshot.metrics.bookedJobs,completedJobs:snapshot.metrics.completedJobs,grossProfit:snapshot.metrics.grossProfit},advertising:{googleAds:{authorization:DAILY_AD_AUTH,performance:snapshot.paidAcquisition.performance,executionConnectionAvailable:true,status:'Windsor Google Ads write actions are connected; execution remains owner-approved and code-budget-guarded.'},meta:{status:'Strategy only — paid Meta Ads connection/budget not authorized'}},beacon:snapshot.beacon,horizon:snapshot.horizon,windsor:snapshot.windsor,boundaries:{organicAutoPublish:false,customerAutoSend:false,automaticPurchases:false,googleAdsDailyMax:10,googleAdsFirstSevenDaysMax:70,metaSpendAuthorized:false}}}

export async function decideMarketingItem({id,decision,edit=''}){if(!id||!['Approved','Rejected'].includes(decision))throw new Error('id and Approved/Rejected decision are required');const rows=await listRecords(TABLES.GROWTH_WORK,{maxRecords:300});const row=rows.find(r=>r.id===id);if(!row||!isMarketingItem(row))throw new Error('Marketing approval item not found');const f=row.fields||{};if(selectName(f['Owner Decision'])!=='Pending')throw new Error(`Marketing item is already ${selectName(f['Owner Decision'])||'resolved'}`);const now=new Date().toISOString(),isSocial=['Instagram','Facebook','Instagram + Facebook'].includes(String(f.Platform||'')),executable=Boolean(f['Execution Connector']&&f['Execution Action']&&f['Execution Payload JSON']),scheduled=Boolean(f['Scheduled At']);const fields={'Owner Decision':decision,'Updated At':now,Status:decision==='Approved'?'Ready for Owner':'Rejected','Execution Status':decision==='Rejected'?'Failed — Owner Attention':executable?(scheduled?'Scheduled':'Approved'):'Approved','Publishing Status':decision==='Rejected'?'Rejected':isSocial?(executable?'Approved — Ready for Connected Publish':'Approved — Publishing Connection Required'):undefined};if(edit){fields['Owner Edit']=clean(edit);if(executable){const edited=withOwnerEdit(String(f['Execution Action']),executionPayload(f['Execution Payload JSON']),edit);validateExecutionSpec({connector:String(f['Execution Connector']),action:String(f['Execution Action']),payload:edited});fields['Execution Payload JSON']=JSON.stringify(edited)}}await updateRecord(TABLES.GROWTH_WORK,id,fields);await logActivity({agent:'ATLAS',actionType:'marketing_owner_decision',status:decision==='Approved'?'Done':'Blocked',detail:`${decision}: ${f['Growth Item']||id}. Executable=${executable}; nothing was executed by the approval request itself.`,consequential:true});return {id,decision,published:false,executable,executionStatus:fields['Execution Status'],publishingStatus:fields['Publishing Status']||null}}
async function runSpecialist(agent,prompt,maxTurns=12){const a=specialist(agent);const result=await runAgent(a,prompt,{maxTurns,observerAgent:a.name});return clean(result.finalOutput||'',12000)}

export async function runDailyMarketingCycle({now=new Date()}={}){
  const date=localDate(now),targetDate=nextLocalDate(now),existing=await listRecords(TABLES.GROWTH_WORK,{maxRecords:300}),already=existing.filter(r=>forDate(r,targetDate)&&isMarketingItem(r));if(already.length>=4)return {date,targetDate,skipped:true,reason:'next_day_marketing_already_prepared',items:already.length};
  await logActivity({agent:'ATLAS',actionType:'daily_marketing_cycle_started',status:'Running',detail:`Preparing marketing work for ${targetDate}. No publishing, customer sending, purchasing, or unauthorized spend.`});
  const shared=`Today is ${date} in Charleston. Finish as much employee work as possible before escalating anything. Prepare primarily for ${targetDate}. Use get_growth_data and verified public evidence. Never fabricate metrics, customers, jobs, reviews, repair results, rankings, group rules, ad results or attribution. "Needs Owner" is reserved for publish/spend/reputation/consequential changes—not the end of analysis. Internal research or implementation handoffs should use ownerApprovalRequired=false. Nothing you save is published automatically. Planned Date must be ${targetDate}.`;const results=[];
  results.push(await runSpecialist(echo,`${shared}\nPrepare at minimum Instagram Feed, Instagram Story, Facebook Feed and Facebook Story. Produce finished copy and a precise visual concept; GhostOS renders ECHO cards into actual branded JPEG assets after storage. Use real repair photos only when verified job photos actually exist; otherwise use general promotional artwork and never imply it depicts a real customer/result. Rotate topics across phone repair, computers, PC builds, Wi-Fi/networking, Business IT/POS, mobile service, education, Charleston-local trust and CTAs. Include CTA, varied hashtags, recommended time and purpose. Publishing always requires owner approval. Windsor supports Facebook Page photo/feed posts and Instagram feed/carousel/story/reel actions. Facebook Story and Facebook organic video/Reels are NOT currently executable through Windsor, so those remain manual.`));
  results.push(await runSpecialist(scout,`${shared}\nPrioritize actual buying-intent opportunities before generic groups: public posts asking for phone/computer/Wi-Fi/business-tech help are higher value than another directory. For any group, verify available rules from live public evidence, research all publicly available rules/evidence yourself, and make additional legitimate attempts before escalating uncertainty. If permission still cannot be established, normally discard it and find a better opportunity; never imply posting is allowed when rules are unavailable or unclear. Only if the opportunity is unusually valuable may you save it for owner judgment, and the next action must be simply "SKIP or REVIEW ANYWAY"—never instruct the owner to research rules. Group posting remains manual/owner-approved. Never pose as a customer, spam, evade rules, or fabricate a lead.`));
  results.push(await runSpecialist(beacon,`${shared}\nUse GA4, Search Console, Google Business Profile and Microsoft Clarity evidence. Clarity is request-limited, so use the existing snapshot once and do not wastefully re-poll it. Do not tell the owner to investigate analytics or implement fixes. Analyze the evidence yourself. When you identify a concrete technical website/attribution/SEO fix, use queue_site_builder_request so BUILDER can investigate and prepare it under existing controls. Save an owner-required marketing card only when a consequential website/business decision is actually ready for approval. Missing Search Console data remains unavailable.`));
  results.push(await runSpecialist(forge,`${shared}\nUse connected Google Ads data. Owner authorization remains hard-limited in code to configured average daily budget <=$10, first-seven-day observed-spend authorization $70, and <=25-mile Charleston/North Charleston radius. Prepare only genuinely policy-eligible B2B work such as Business IT, POS, business networking/Wi-Fi, or device deployment. Never disguise prohibited consumer repair/support services. Only actual publish/spend decisions belong in ownerApprovalRequired=true.`));
  results.push(await runSpecialist(horizon,`${shared}\nJudge channels by spend -> lead -> booked job -> completed job -> revenue -> parts cost -> gross profit -> CAC/cash. Engagement without customers is not success. If attribution is missing, do NOT tell the owner to implement UTMs/GCLID. Record the internal requirement with ownerApprovalRequired=false and route the need to BEACON/BUILDER through ATLAS; only escalate a real business allocation decision once evidence supports it.`));
  await logActivity({agent:'ATLAS',actionType:'daily_marketing_cycle_completed',status:'Done',detail:`Marketing preparation completed for ${targetDate}. ATLAS kept routine research/analytics work away from the owner.`});return {date,targetDate,skipped:false,specialists:['ECHO','SCOUT','BEACON','FORGE','HORIZON'],externalActionsTaken:false,results};
}
export {DAILY_AD_AUTH,localDate};
